
services:

  postgres:
    container_name: grade-service-postgres
    image: postgres:latest
    restart: unless-stopped
    ports:
      - "5030:5432"
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: grade-service-db
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: